# Student Fee Receipt — Field List

Issued to a student the moment the Trust approves their scholarship payment (see
`PATCH /:id/payment/approve` in `server/src/routes/applications.js`), generated by
`server/src/studentFeeReceipt.js`. Itemizes the money: course fee, discount, and what
was actually paid.

Numbered sequentially per year, e.g. `NGF/2026/00001`.

---

## Letterhead (fixed)

| Field | Value |
|---|---|
| Trust Name | NEXTGEN SOLUTIONS EDUCATIONAL TRUST (all caps) |
| Address | 4/1023-D, Ayyalu Meenakshi Nagar, Udumalpet - 642 126, Tiruppur (Dt.), Tamil Nadu |
| Contact | nextgencollegesolutions@gmail.com \| 93423 79043 / 97902 13628 |
| Document Title | RECEIPT |
| Watermark | Faint, centered `src/assests/Logo.png`, drawn behind all content |

---

## Receipt Details

| Field | Source |
|---|---|
| Receipt No. | Auto-generated, e.g. `NGF/2026/00001` |
| Date | Date the receipt is issued (payment approval date) |
| Received from | `application.fullName`, upper-cased for display |
| Course | `application.examName` (falls back to `application.college.degree`) |
| Mobile | `application.mobile` |
| Email | `application.email` |
| Amount (in words) | Amount paid, spelled out — e.g. "Ten Thousand Only" |

---

## Fee Breakdown

The itemized part unique to this receipt:

| Field | Source | Notes |
|---|---|---|
| Course Fee | `application.courseFee` | Original fee before any concession |
| Concession | `application.finalApprovedConcession` | As a percentage |
| Discount Amount | `courseFee − amountDue` | Computed, never entered directly |
| Amount Payable | `payment.amountDue` | Server-computed from course fee × concession — never trusted from the client |
| **Amount Paid** | `payment.amountPaid` | Boxed/highlighted on the receipt |

---

## Payment Details

| Field | Source |
|---|---|
| Payment Mode | `payment.paymentMethod` (upi / bank_transfer / other), upper-cased with underscores replaced by spaces — e.g. `BANK TRANSFER` |
| Payment Date | `payment.paymentDate` |
| UTR / Transaction ID | `payment.transactionId` |
| Purpose | "\[Course Name\] Fee" |

---

## Note & Signatory

| Field | Value |
|---|---|
| Note | "This fee is collected for educational purpose only. Not refundable." |
| Signature | `src/assests/signature.png`, embedded above the signatory line |
| Signed For | NEXTGEN SOLUTIONS EDUCATIONAL TRUST — Authorized Signatory |

---

## Access

- **Admin:** `GET /api/applications/:id/payment/receipt-signed-url` — view/download from the
  Fee Receipt section on the application detail page. `POST /api/applications/:id/payment/receipt/generate`
  retries generation if it failed on approval.
- **Student:** same signed-url endpoint (dual admin/owner access) — "Download Fee Receipt"
  button on the `/payment` page once payment is approved.

*If the generated receipt's layout or fields change, update this list to match
`server/src/studentFeeReceipt.js`.*
